@CLAUDE.md

If your tooling did not just resolve that import, read `CLAUDE.md` in this directory now and follow
it. It is the single source of truth for working in this repo — the issue tracker, the triage
labels, the domain docs, and the checkpoint rules that say when to stop and ask Sean rather than
push through. Nothing is duplicated here, so skipping it means working without any of it.
